/**
 * Port of Angular `SignalRHelper.ts`. Stub — wire up `@microsoft/signalr` when needed.
 */
export const signalRHelper = {
  initSignalR(_onConnected?: () => void): void {
    // TODO: connect to /signalr hub when the real-time module is enabled.
  },
};
