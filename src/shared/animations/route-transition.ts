/**
 * Replaces Angular `routerTransition.ts`. Tailwind-class-based fade for new routes.
 * Apply `routeTransition` className on the root of each page to fade-in on mount.
 */
export const routeTransition = 'animate-in fade-in-50 duration-200';
